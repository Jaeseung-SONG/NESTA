# Reproducibility Verified

The simulation_study package reproduces the finalized aggregate metrics within the prespecified tolerance and generates the reviewer-facing delta_NESTA threshold sensitivity outputs.

The default public workflow is `Rscript simulation_study/run_simulation_study.R --report-dir <new_result_dir>`.
